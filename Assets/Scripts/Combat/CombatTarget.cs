using UnityEngine;
using RPG.Core;

namespace RPG.Combat
{
    [RequireComponent(typeof(Health))]  //This will add a Health component if it is not already there
    public class CombatTarget : MonoBehaviour
    {
        
    }
}